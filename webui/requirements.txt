# Web UI dependencies. Install into the same env you run the analysis scripts
# from (e.g. `conda activate ratinabox && pip install -r webui/requirements.txt`),
# since script/param introspection imports the repos' own modules.
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
pyyaml==6.0.2
# jinja2 and pandas are already pulled in by ratinabox/environment.yml
